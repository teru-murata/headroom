{
  "provider": "synthetic",
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "checkout.lookup",
        "description": "Look up checkout state by customer id.",
        "parameters": {
          "type": "object",
          "required": ["customer_id"],
          "properties": {
            "customer_id": {
              "type": "string",
              "description": "Stable customer identifier."
            },
            "include_invoices": {
              "type": "boolean",
              "description": "Whether to include invoice rows."
            }
          },
          "additionalProperties": false
        }
      }
    },
    {
      "name": "checkout.update_note",
      "input_schema": {
        "type": "object",
        "required": ["checkout_id", "note"],
        "properties": {
          "checkout_id": {"type": "string"},
          "note": {"type": "string"}
        }
      }
    }
  ],
  "x-generated-doc": [
    "This generated section repeats schema docs.",
    "This generated section repeats schema docs.",
    "This generated section repeats schema docs.",
    "This generated section repeats schema docs.",
    "This generated section repeats schema docs.",
    "This generated section repeats schema docs.",
    "This generated section repeats schema docs.",
    "This generated section repeats schema docs."
  ]
}
